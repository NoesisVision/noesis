{
  "name": "@noesis-vision/claude-code-plugin",
  "version": "0.1.0-beta.2",
  "private": false,
  "description": "Noesis Claude Code plugin — skills for preparing and validating MCP tool payloads",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NoesisVision/noesis.git",
    "directory": "plugins/claude-code"
  },
  "files": [
    ".claude-plugin/plugin.json",
    ".mcp.json",
    "contracts",
    "scripts",
    "servers",
    "skills"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "generate": "bun run tools/generate-references.ts && bun run tools/copy-contracts.ts && bun run tools/stamp-plugin-version.ts",
    "bundle": "bun run tools/bundle-server.ts",
    "validate": "bun run scripts/validate.ts",
    "test": "bun test",
    "check-types": "tsc --noEmit",
    "bump": "bun run tools/bump-version.ts",
    "release:beta": "bun run tools/release-beta.ts",
    "publish:beta": "bun publish --tag beta",
    "prepublishOnly": "bun run generate && bun run bundle"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@repo/mcp-contracts": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
