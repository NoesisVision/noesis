{
  "name": "@noesis-vision/claude-code-plugin",
  "version": "0.1.0-beta.4",
  "private": false,
  "description": "Noesis Claude Code plugin — the contract sources an agent reads and the launch config for the Noesis service",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NoesisVision/noesis.git",
    "directory": "plugins/claude-code"
  },
  "files": [
    ".claude-plugin/plugin.json",
    ".mcp.json",
    "contracts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "generate": "bun run tools/stamp-plugin-version.ts",
    "build": "bun run tools/copy-contracts.ts contracts",
    "prepack": "bun run build",
    "test": "bun test",
    "check-types": "tsc --noEmit",
    "bump": "bun run tools/bump-version.ts",
    "release:beta": "bun run tools/release-beta.ts",
    "publish:beta": "bun publish --tag beta",
    "prepublishOnly": "bun run generate"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
