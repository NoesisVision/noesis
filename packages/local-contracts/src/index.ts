// Shared DTOs between the server and local apps.
// Add new contracts here (or in dedicated files re-exported from this barrel).
// DTOs shared with the ui/mcp contracts live in @repo/shared-contracts.

export * from '@repo/shared-contracts';
