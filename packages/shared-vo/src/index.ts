// Value objects shared across the workspace: small runtime helpers with no
// dependencies beyond the runtime, as opposed to `@repo/shared-contracts`,
// which stays declarative (decision D4).

export * from './uuid.js';
