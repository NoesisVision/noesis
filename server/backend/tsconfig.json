{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Same alias the frontend uses for this source, so backend files resolve
    // alike in both programs (decision D3).
    "paths": {
      "#backend/*": ["./src/*"]
    }
  }
}
